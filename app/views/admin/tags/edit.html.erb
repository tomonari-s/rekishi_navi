<div class="container">
  <div class ="col-md-3">
    <h4 class ="bg-light", align="center">タグ編集</h4>
  </div>
  
    <div class ="col-md-8">
      <%= form_with model: @tag, url: admin_tag_path(@tag.id), method: :patch  do |f| %>
        <%= f.label :ジャンル名, class:"mr-5 mt-5 mb-5" %>
        <%= f.text_field :name,autocomplete: "name", class:"mr-5"  %>
        <%= f.submit "変更を保存", class:"btn btn-success" %>
      <% end %>
    </div>
  </div>
</div>